import React from "react";
import { VideoComposition, type VideoCompositionProps } from "./VideoComposition";

export const ShortsVideo: React.FC<VideoCompositionProps> = (props) => (
  <VideoComposition {...props} />
);
